{{!-- <li>
    {{name}}
    {{!-- Devour to Ate Button --}}
    <button class="eat-burger" data-id="{{id}}" data-neweat="{{ate}}">
        {{#if ate}}Devour! {{else}}Order Another{{/if}}
    </button>
    {{!-- Delete Button --}}
    <button class="delete" data-id="{{this.id}}">Delete</button>
</li> --}}